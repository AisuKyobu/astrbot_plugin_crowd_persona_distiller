# Product

## Register

product

## Users

AstrBot 插件管理员。他们部署了「群友蒸馏师」插件，通过浏览器打开 AstrBot WebUI 的插件管理面板来：
- 查看群友聊天数据，触发 LLM 蒸馏生成人格画像
- 管理称呼映射（QQ号 ↔ 昵称别名）
- 按群配置扮演回复模式（随机/指定/关闭）
- 上传 qq-chat-exporter 导出的聊天记录导入历史数据

使用场景：PC 浏览器，偶尔打开进行管理操作，非高频日常工具。

## Product Purpose

群友蒸馏师 的 Web 管理面板。核心任务链路：
1. 选群 → 看群友列表 → 蒸馏 → 得到人格画像
2. 配置扮演规则 → 让 Bot 在群里自然扮演群友

成功标准：管理员能用最少的点击完成蒸馏、编辑人格、调整配置，状态反馈清晰（加载中/成功/失败/空数据各有明确视觉表现）。

## Brand Personality

**清晰、高效、可靠**

- **清晰**：每个操作入口明确，不做隐藏功能。卡片信息一目了然。
- **高效**：高频操作一键可达（蒸馏、编辑人格、切换群）。减少不必要的确认步骤。
- **可靠**：所有异步操作有明确的状态反馈（loading / success / error / empty）。不出静默失败。

## Anti-references

- 不做暗色主题（因 AstrBot 插件页仅支持亮色）
- 不做花哨的渐变背景、毛玻璃、大装饰图
- 不做过度动画（不弹跳、不弹性、不旋转）

## Design Principles

1. **状态可见**：每个操作（蒸馏、导入、保存）必须有 loading → success/error 的可视化链路，不让用户猜测是否生效。
2. **减少认知负荷**：信息按优先级分层。已蒸馏 / 待蒸馏 / 消息不足 三个区域有明确视觉区分，不用等量卡片堆砌。
3. **操作就近**：编辑、删除、增量更新、修正人格 — 所有对同一个 persona 的操作集中在弹窗 footer，不散落在不同页面。
4. **数据驱动**：进度条、消息数、最后蒸馏时间等量化信息始终可见，帮助管理员判断下一步行动。

## Accessibility & Inclusion

- 无特殊无障碍要求，常规对比度即可
- 不依赖颜色作为唯一信息传达手段（关键状态配合文字/图标）
